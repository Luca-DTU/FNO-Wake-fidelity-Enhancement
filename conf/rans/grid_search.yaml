defaults:
  - _self_
  - base
### OVVERRIDES
skip_errors: True
###
hydra:
  mode : MULTIRUN
  sweeper:
    params:
      data_source.name: choice(rans_prescaled_on_resolution,rans_prescaled_independently,rans)
      TFNO.n_layers: choice(4,8,12,16)
      train.batch_size: choice(16,32,64,128)

defaults:
 - lightweight
data_source:
  train_args:
    inputs: [A_AWF,Fx]
    path: data/RANS_Newton/

data_format:
  encode_input: True

TFNO : 
  non_linearity : sigmoid #gelu # use same capitalisation as torch.nn.functional